name: ClickUp to Telegram Notifications

on:
  schedule:
    # هر 5 دقیقه اجرا می‌شود
    - cron: '*/5 * * * *'
  workflow_dispatch: # اجرای دستی

jobs:
  check-comments:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check ClickUp and Send to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CLICKUP_API_TOKEN: ${{ secrets.CLICKUP_API_TOKEN }}
          CLICKUP_TEAM_ID: ${{ secrets.CLICKUP_TEAM_ID }}
        run: |
          #!/bin/bash
          
          # تابع تبدیل به تاریخ شمسی
          get_jalali_date() {
            python3 << 'PYTHON'
          from datetime import datetime
          def gregorian_to_jalali(gy, gm, gd):
              g_d_m = [0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334]
              if gy > 1600:
                  jy = 979
                  gy -= 1600
              else:
                  jy = 0
                  gy -= 621
              gy2 = gy + 1 if gm > 2 else gy
              days = (365 * gy) + (gy2 + 3) // 4 - (gy2 + 99) // 100 + (gy2 + 399) // 400 - 80 + gd + g_d_m[gm - 1]
              jy += 33 * (days // 12053)
              days %= 12053
              jy += 4 * (days // 1461)
              days %= 1461
              if days > 365:
                  jy += (days - 1) // 365
                  days = (days - 1) % 365
              if days < 186:
                  jm = 1 + days // 31
                  jd = 1 + days % 31
              else:
                  jm = 7 + (days - 186) // 30
                  jd = 1 + (days - 186) % 30
              return jy, jm, jd
          now = datetime.now()
          jy, jm, jd = gregorian_to_jalali(now.year, now.month, now.day)
          months = ["فروردین", "اردیبهشت", "خرداد", "تیر", "مرداد", "شهریور", "مهر", "آبان", "آذر", "دی", "بهمن", "اسفند"]
          print(f"{jd} {months[jm-1]} {jy} - ساعت {now.strftime('%H:%M')}")
          PYTHON
          }
          
          # دریافت timestamp 5 دقیقه پیش (میلی‌ثانیه)
          FIVE_MIN_AGO=$(($(date +%s) * 1000 - 300000))
          
          echo "🔍 بررسی کامنت‌های جدید..."
          
          # دریافت تسک‌ها
          TASKS=$(curl -s "https://api.clickup.com/api/v2/team/${CLICKUP_TEAM_ID}/task?limit=20" \
            -H "Authorization: ${CLICKUP_API_TOKEN}")
          
          # پردازش با Python
          python3 << PYTHON
          import json
          import os
          import urllib.request
          import urllib.parse
          from datetime import datetime
          
          def gregorian_to_jalali(gy, gm, gd):
              g_d_m = [0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334]
              if gy > 1600:
                  jy = 979
                  gy -= 1600
              else:
                  jy = 0
                  gy -= 621
              gy2 = gy + 1 if gm > 2 else gy
              days = (365 * gy) + (gy2 + 3) // 4 - (gy2 + 99) // 100 + (gy2 + 399) // 400 - 80 + gd + g_d_m[gm - 1]
              jy += 33 * (days // 12053)
              days %= 12053
              jy += 4 * (days // 1461)
              days %= 1461
              if days > 365:
                  jy += (days - 1) // 365
                  days = (days - 1) % 365
              if days < 186:
                  jm = 1 + days // 31
                  jd = 1 + days % 31
              else:
                  jm = 7 + (days - 186) // 30
                  jd = 1 + (days - 186) % 30
              return jy, jm, jd
          
          def format_date(ts):
              try:
                  ts = int(ts)
                  if ts > 10000000000:
                      ts = ts / 1000
                  dt = datetime.fromtimestamp(ts)
                  jy, jm, jd = gregorian_to_jalali(dt.year, dt.month, dt.day)
                  months = ["فروردین", "اردیبهشت", "خرداد", "تیر", "مرداد", "شهریور", "مهر", "آبان", "آذر", "دی", "بهمن", "اسفند"]
                  return f"{jd} {months[jm-1]} {jy} - ساعت {dt.strftime('%H:%M')}"
              except:
                  return "نامشخص"
          
          def send_telegram(text):
              token = os.environ.get('TELEGRAM_BOT_TOKEN')
              chat_id = os.environ.get('TELEGRAM_CHAT_ID')
              if not token or not chat_id:
                  return
              url = f"https://api.telegram.org/bot{token}/sendMessage"
              data = urllib.parse.urlencode({
                  'chat_id': chat_id,
                  'text': text,
                  'parse_mode': 'Markdown'
              }).encode()
              try:
                  req = urllib.request.Request(url, data=data)
                  urllib.request.urlopen(req, timeout=10)
                  print("✅ پیام ارسال شد")
              except Exception as e:
                  print(f"❌ خطا: {e}")
          
          def get_comments(task_id):
              token = os.environ.get('CLICKUP_API_TOKEN')
              url = f"https://api.clickup.com/api/v2/task/{task_id}/comment"
              req = urllib.request.Request(url, headers={'Authorization': token})
              try:
                  with urllib.request.urlopen(req, timeout=10) as response:
                      return json.loads(response.read()).get('comments', [])
              except:
                  return []
          
          # Load tasks
          tasks_json = '''$TASKS'''
          try:
              data = json.loads(tasks_json)
              tasks = data.get('tasks', [])
          except:
              tasks = []
          
          five_min_ago = $FIVE_MIN_AGO
          new_comments = 0
          
          for task in tasks[:15]:
              task_id = task.get('id')
              task_name = task.get('name', 'نامشخص')
              
              comments = get_comments(task_id)
              
              for comment in comments[:3]:
                  comment_date = int(comment.get('date', 0))
                  
                  # فقط کامنت‌های 5 دقیقه اخیر
                  if comment_date > five_min_ago:
                      user = comment.get('user', {})
                      username = user.get('username') or user.get('email', 'نامشخص')
                      text = comment.get('comment_text', '')
                      
                      if text:
                          message = f"""📋 **در تسک «{task_name}»**

          👤 **{username}** نوشته:

          💬 {text}

          🕐 {format_date(comment_date)}"""
                          
                          send_telegram(message)
                          new_comments += 1
          
          print(f"📊 {new_comments} کامنت جدید یافت شد")
          PYTHON

